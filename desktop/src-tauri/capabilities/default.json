{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main and splash windows",
  "windows": ["main", "splash"],
  "permissions": [
    "core:default",
    "notification:default",
    "dialog:default",
    "autostart:default"
  ]
}
